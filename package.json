{
  "name": "subsidy-dropoff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Subsidy Dropoff - find the income at which your ACA premium tax credit disappears",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "etl": "tsx src/etl/run.ts",
    "dev": "tsx src/dev-server.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "homepage": "https://subsidydropoff.com"
}
